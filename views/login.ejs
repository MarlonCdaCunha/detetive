<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DETETIVE - Login</title>
    <style>
        body {
            font-family: 'Georgia', serif;
            margin: 0;
            padding: 0;
            min-height: 100vh;
            background: linear-gradient(rgba(0, 0, 0, 0.5), rgba(0, 0, 0, 0.5)),
                        url('/images/mansion-interior.png') center/cover;
            color: #e0c088;
        }

        .container {
            max-width: 400px;
            margin: 50px auto;
            padding: 30px;
            background: rgba(43, 27, 23, 0.85);
            border: 2px solid #8b0000;
            border-radius: 10px;
            box-shadow: 0 0 30px rgba(0, 0, 0, 0.7);
            backdrop-filter: blur(5px);
        }

        h1 {
            text-align: center;
            color: #d4af37;
            font-size: 2.5em;
            margin-bottom: 30px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }

        .form-group {
            margin-bottom: 20px;
        }

        label {
            display: block;
            margin-bottom: 8px;
            color: #d4af37;
            font-size: 1.1em;
        }

        input {
            width: 100%;
            padding: 12px;
            border: 1px solid #8b0000;
            background: rgba(255, 255, 255, 0.9);
            border-radius: 5px;
            font-size: 1em;
            color: #2b1b17;
            box-sizing: border-box;
        }

        input:focus {
            outline: none;
            border-color: #d4af37;
            box-shadow: 0 0 5px #d4af37;
        }

        button {
            width: 100%;
            padding: 12px;
            background: #8b0000;
            color: #e0c088;
            border: none;
            border-radius: 5px;
            font-size: 1.1em;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        button:hover {
            background: #a52a2a;
        }

        .register-link {
            text-align: center;
            margin-top: 20px;
        }

        .register-link a {
            color: #d4af37;
            text-decoration: none;
            transition: color 0.3s;
        }

        .register-link a:hover {
            color: #ffd700;
        }

        .magnifying-glass {
            text-align: center;
            font-size: 3em;
            margin-bottom: 20px;
            color: #d4af37;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="magnifying-glass">🔍</div>
        <h1>DETETIVE</h1>
        
        <% if (typeof error !== 'undefined') { %>
            <div style="color: #ff4444; text-align: center; margin-bottom: 20px;">
                <%= error %>
            </div>
        <% } %>
        
        <form action="/login" method="POST">
            <div class="form-group">
                <label for="username">Nome do Investigador:</label>
                <input type="text" id="username" name="username" required>
            </div>
            <div class="form-group">
                <label for="password">Código Secreto:</label>
                <input type="password" id="password" name="password" required>
            </div>
            <button type="submit">Entrar na Mansão</button>
        </form>
        
        <div class="register-link">
            <p>Novo Investigador? <a href="/">Registre-se para Investigar</a></p>
        </div>
    </div>
</body>
</html>