<!DOCTYPE html>
<html>
<head>
    <title>Nova Investigação - Detetive</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: #2b1b17;
            color: #fff;
            margin: 0;
            padding: 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
            padding: 20px;
            background: rgba(139, 0, 0, 0.3);
            border-radius: 10px;
            border: 2px solid #8b0000;
        }

        .header h1 {
            margin: 0;
            font-size: 2.5em;
            color: #fff;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }

        .card {
            background: rgba(43, 27, 23, 0.9);
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.3);
            overflow: hidden;
            padding: 20px;
        }

        .start-button {
            display: block;
            width: 200px;
            margin: 30px auto;
            padding: 15px 30px;
            background: #8b0000;
            border: none;
            border-radius: 5px;
            color: white;
            font-size: 1.2em;
            cursor: pointer;
            transition: background 0.3s;
        }

        .start-button:hover {
            background: #a00000;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Nova Investigação</h1>
        </div>
        <div class="card">
            <button class="start-button" onclick="window.location.href='/partida'">Iniciar Investigação</button>
        </div>
    </div>
</body>
</html>